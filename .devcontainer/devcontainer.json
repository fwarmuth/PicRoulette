{
	"name": "Flutter dev",
    "build": {
        "dockerfile": "Dockerfile",
        "target": "base",
        "context": "..",
        "runArgs": [
            // Mount the host machine's /dev/kvm to /dev/kvm in the container
            "--network", "host",
        ]
    }
}
